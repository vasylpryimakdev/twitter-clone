export const LoginPage = () => {
  return (
    <div>
      <h2>Login</h2>

      <input placeholder="email" />
      <input placeholder="password" type="password" />

      <button>Login</button>
    </div>
  );
};
